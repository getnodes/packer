name: Packer
on:
  workflow_dispatch:
    inputs:
      pritunl:
        required: true
        type: boolean
      k3s-lb-master:
        required: true
        type: boolean
      k3s-lb-backup:
        required: true
        type: boolean
      k3s-server:
        required: true
        type: boolean
      k3s-agent:
        required: true
        type: boolean
jobs:
  packer:
    runs-on: ubuntu-latest
    env:
      HCLOUD_TOKEN: ${{ secrets.HCLOUD_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Packer
        uses: hashicorp/setup-packer@v3
        with:
          version: "1.15.3"
      - run: rm pritunl.pkr.hcl
        if: ${{ inputs.pritunl == false }}
      - run: rm k3s-lb-master.pkr.hcl
        if: ${{ inputs.k3s-lb-master == false }}
      - run: rm k3s-lb-backup.pkr.hcl
        if: ${{ inputs.k3s-lb-backup == false }}
      - name: Init
        run: packer init .
      - name: Lint
        run: packer fmt -check .
      - name: Validate
        run: packer validate .
      - name: Build
        run: packer build .
